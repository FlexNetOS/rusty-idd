# Secret-scan allowlist (rusty-idd validate). One path substring per line.
# A file whose relative path contains an entry is exempt from secret-pattern
# findings. Use ONLY for verified placeholders: secret-DETECTION regexes, test
# fixtures, and example tokens — never to hide a real secret. See ADR-0018.
#
# imports/prompt_hub: privacy.rs is a secret-DETECTION module (its own provider
# token-detection regexes) and context_gatherer.rs carries detection patterns +
# fixtures. Verified 2026-06-23: no real secrets in imports/ (no private keys, no
# live credentials) — these matches are detection regexes and fake test values.
imports/prompt_hub/prompt-hub/src/privacy.rs
imports/prompt_hub/prompt-hub/src/context_gatherer.rs
