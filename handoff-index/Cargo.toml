[package]
name = "handoff-index"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "handoff-index — repo navigation maps (hf index) + the task DAG (hf plan), PRD §8/§9, peeled from hf"

[dependencies]
handoff-core = { path = "../handoff-core" }
work-order = { path = "../work-order" }
serde_json = "1"

[lints]
workspace = true
