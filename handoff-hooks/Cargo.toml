[package]
name = "handoff-hooks"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "handoff-hooks — the typed lifecycle hook contract + runner (HFTASK-0052, PRD §18), peeled from hf"

[dependencies]
handoff-core = { path = "../handoff-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[lints]
workspace = true
