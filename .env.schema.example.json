{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "rusty-idd Environment Schema",
  "type": "object",
  "properties": {
    "RUST_LOG": {
      "type": "string",
      "description": "Optional local logging filter for Rust components."
    },
    "NO_COLOR": {
      "type": "string",
      "description": "Optional flag requesting plain terminal output."
    }
  },
  "additionalProperties": true
}
