[package]
name = "handoff-intake"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "handoff-intake — the front-door intake/dispatch verbs (prompt_hub SwarmBundle → handoff.task.v1), peeled from hf"

[dependencies]
handoff-core = { path = "../handoff-core" }
work-order = { path = "../work-order" }
serde_json = "1"

[lints]
workspace = true
