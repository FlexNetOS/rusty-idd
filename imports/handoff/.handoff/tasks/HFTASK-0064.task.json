{
  "schema": "handoff.task.v1",
  "id": "HFTASK-0064",
  "title": "hf doctor fail-closed invariant sweep + stale-lock self-heal (assert the FAIL-OPEN class is closed)",
  "status": "done",
  "priority": "P1",
  "objective": "The systemic guard (5th target, harness-evolution L7/L10): a point fix per surface isn't enough — `hf doctor` must ASSERT the whole fail-open class stays closed AND auto-heal the one liveness wedge. Extend cmd_doctor to: (a) enumerate every `tasks/*.task.json` and FAIL (DEGRADED + exit 1) if any card on disk is absent from `hf status` (catches the load_tasks silent-drop that hid card #95 a whole session); (b) assert ledger replay + witness-chain verify with NO empty/default fallback masking a read error; (c) detect a provably-dead `*.rvf.lock` and reclaim it via HFTASK-0062 (witnessed `lock_reclaimed`), REFUSING live/unverifiable holders; (d) `hf doctor --json` structured report. Unit-tested: dead-reclaim, live-refusal, missing-card-fails, empty-status-fails. Depends on 0062 (consume reclaim) + the loud-load fix in 0057 (assert). Detection-only form can ship first (additive, no-downgrade).",
  "path_scope": [
    "spike/**",
    "handoff/**"
  ],
  "acceptance_criteria": [
    "hf doctor fail-closed invariant sweep + stale-lock self-heal (assert the FAIL-OPEN class is closed): implemented + cargo test green + checkpointed"
  ],
  "test_commands": [
    "cargo test -p hf try_parse_card",
    "cargo test -p ledger lock"
  ],
  "dependencies": [
    "HFTASK-0062",
    "HFTASK-0057"
  ],
  "blocked_by": [],
  "allows_network": false,
  "allows_dependency_addition": true,
  "correlation_id": "handoff-buildout",
  "role": "implementer",
  "intent_lock": {
    "objective_hash": "blake3:0c765f0dece845c80dd1184e2e35dbddbaaaea371c044a5c452ce8ff5092f428",
    "path_scope_hash": "blake3:2ffa7a442ff7ccdaf5c3a7ddad4cdf9b388fc5ed01dde27996a7c9f7d0e2f221",
    "acceptance_hash": "blake3:e46401d98013bd2aafe04eddc79baf9603d70501d2f495ba61050c7ae0c26980",
    "constraint_hash": "blake3:8784b1d26589c76eb98d3c07382f67b1a2f013485080541874afccd08644fad6",
    "northstar_revision": "blake3:aa161a7ffed934d6be955de6b80125bb77bddad718465077b94f9c73e197df70"
  }
}