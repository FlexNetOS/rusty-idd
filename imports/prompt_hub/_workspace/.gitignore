# Per-run external-runner logs (ephemeral; truth is in commits + state files)
ralph-run-*.log
*.log
# Local-only terminal sentinels
STOP
# Local Python virtual environments used for generating test fixtures
onnx_venv/
