# SESSION HANDOFF — prompt_hub construction loop (2026-06-13)

closed_utc: 2026-06-13 (wrap-up)   ·   resume_command: `/prompt-loop resume`
state_backend: `.handoff/` Continuity Ledger Kernel (member model; packet via `hf fleet render prompt_hub`)
orchestrator_phase: between cycles (last cycle committed + merged)
last_item: PHTASK-0035 (done)   ·   next_item: PHTASK-0036 (or current top `status:"backlog"` card)
gate_status: PASS (every landed PR green; auto-merged)

## Landed this session (all merged to main)
- #80 feat(server): evolve_prompt route
- #81 feat(hub): wire tokens.rs (count_prompt_tokens / estimate_prompt_cost)
- #82 chore(handoff): migrate _workspace -> .handoff (40 cards, packet, capsule, history)
- #83 chore(handoff): copy full kernel .handoff substrate (hooks/policies/skills/decisions)
- #84 fix(deps): default-features build (password-hash 0.6->0.5) + CI default-build job  [PHTASK-0028]
- #85 feat(hub): wire MultiModalInput (process_input)                                    [PHTASK-0031]
- #86 chore(loop): enforce NO-DOWNGRADE / upgrade-only (recast cards + harness rules)
- #87 feat(hub): wire TemplateEngine into rendering (render_prompt/lint_template)        [PHTASK-0033]
- #89 feat(server): 4 routes (tokens/cost/render/input-process)                          [PHTASK-0035]

> NOTE: concurrent sessions are also building prompt_hub (e.g. #92 budget ungate landed
> mid-session). The card done/total count drifts as they land — trust `hf fleet render`.

## Decisions & dead-ends (so they aren't re-litigated)
- **NO DOWNGRADES — UPGRADE ONLY** (owner, standing): dead-looking code is an INCOMPLETE
  feature to COMPLETE; never remove/gate-out/stub. Encoded in prompt-loop SKILL.md,
  backlog-curator agent, feature-build skill. Cards are completion-only.
- **Stale audit backlog**: the migrated 2026-06-07 audit claims ("empty match arms",
  "no impls") are STALE — multimodal_input.rs + templates.rs were already implemented;
  the gap was WIRING. Verify code-vs-prose per card.
- **Worktree-per-cycle is mandatory** — concurrent sessions switch branches under you.
- **hf member-model**: `hf fleet render prompt_hub` targets the MAIN checkout, never a
  worktree → a worktree cycle can't render its own packet. Commit cards+active+code only;
  the packet is derived and regenerates on resume. (See LESSONS below.)

## Findings (open)
- PHTASK-0040 (blocked): default `AgentIdentity` lacks Write for programmatic `PromptHub::new()`
  callers (server `default_agent()` is fine). Documented workaround: `AgentIdentity::local_operator()`.
- `--no-default-features` (zero features) build fails pre-existing (`SafeDeployer`, hub.rs:416) —
  out of scope for the default-features card; a future completion item if pursued.

## icm_stored (topics written this session — resume should recall these)
- `context-prompt_hub` (per-cycle + session summary)
- `decisions-prompt_hub` (lessons L1-L5; no-downgrade)
- `preferences` (NO-DOWNGRADE owner directive, critical)

## verify_on_resume (run FIRST to confirm green)
```bash
cd ~/Desktop/meta && hf fleet render prompt_hub && grep Done: prompt_hub/.handoff/packets/latest.md
cd ~/Desktop/meta/prompt_hub && cargo check --workspace --all-features   # in a worktree, not the shared checkout
```
