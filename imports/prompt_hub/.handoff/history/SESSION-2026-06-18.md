# SESSION-2026-06-18 — prompt_hub completeness handoff

**Type:** handoff / queue-reconciliation (no feature code changed; queue made complete)
**Repo:** `prompt_hub` @ `main` (synced; commit base d07216c)
**Owner ask:** *"Prepare handoff with a deep search for the original prompt_hub prompt and
design | follow the backlog and any session handoff | the goal is to verify all work that is
not done is properly identified and lined up in the que."*

---

## 1. North Star (unchanged)
A non-technical user makes any request; prompt_hub transforms, communicates, and delivers it
as intended (SwarmBundle → handoff.task.v1). Pure-Rust, `#![forbid(unsafe_code)]`, libsql
(not C SQLite), Rust 2024 / MSRV 1.91.1.

## 2. State precedence (authoritative)
`Git > .handoff/ledger.db > tasks/*.task.json > active.md > packets/latest.md`.
Cold-start: read `.handoff/context/capsule.json`, then `hf resume`. `hf status` is truth.

## 3. What the deep search found (the design corpus)
The original design intent lives in the repo root (all present in `main`):
- **`SPEC.md`** (196 L) — the workspace spec: crate layout, feature flags, hub API, DB schema,
  CLI command list, server surface, key types. **This is the design source of truth.**
- **`.prompt.md`** (371 L) — a *prompt-template library* (review/feature/debug/migration
  templates), NOT a build spec. Useful as agent guidance, not a feature backlog.
- **`TODO.md`, `SESSION.md`, `VERIFICATION_REPORT.md`** — historical session notes.
- **`_workspace/{backlog,HANDOFF,loop_state,DEPRECATED}.md`** — the *old* prompt-loop crew
  state, **migrated 2026-06-13** to `.handoff/` (cards + packet + ledger). These files are now
  thin DEPRECATED pointers; originals archived verbatim under
  `.handoff/history/_workspace-archive/`. Migration is clean — do not add state to `_workspace/`.
- The derived `.handoff/packets/latest.md` and `.handoff/active.md` were **stale** (frozen at
  cycle 86/87, "46/46 done, project: handoff"). Regenerated this session via
  `hf fleet render prompt_hub`.

## 4. Completeness verdict — the queue was NEARLY complete; 6 gaps were untracked
A full reconciliation (every `pub` hub method vs every server route vs the union of route-cards;
a stub/passthrough grep sweep; a SPEC feature-flag audit) confirmed:
- **Route epic is airtight.** |H_user|=156, |R routed|=32, |U uncovered|=124, and the route
  sub-cards 0057–0063 enumerate exactly 124 capabilities with **zero phantom entries**. Only
  **one** real capability had slipped the net: `set_malware_scan_config` (hub.rs:3124) — now
  folded into PHTASK-0063.
- **3 untracked stubs** (verified against source this session):
  - `lock.rs:149` `spawn_sweep_task` — no-op; the `tokio::spawn … DELETE FROM locks WHERE
    expires_at < now` is commented out → **expired locks never swept** (silent leak). → **0066**
  - `prompthub/src/commands/junie.rs:24` `JunieCommand::Task` — print-only, `_hub` unused,
    `// Real logic would go here`. → **0067**
  - `prompthub/src/tui.rs:8` `run_tui` — prints "feature stub …", 5 deps pulled, none used. → **0068**
- **2 hollow feature flags** (declared + deps pulled, ZERO `#[cfg]`/usages in src):
  `tls` (→tokio-rustls, server binds plain TCP) → **0069**; `tokenizers` (→dep:tokenizers,
  dead) → **0070**.
- **2 SPEC-promised-but-absent flags:** `sqlcipher` (encryption-at-rest) and `ffi` (C ABI) exist
  nowhere in any Cargo.toml or src. → **0071** (implement OR reconcile SPEC.md).

All 7 fixes filed as cards (intent_lock blake3-valid; `hf doctor` = health OK, 21 witnessed
events, no dropped cards). **Queue is now complete: every not-done item has a card.**

## 5. The queue — 18 remaining of 71 (53 done)
Run `hf resume` / `hf claim <id>` to work. No card is blocked (all `blocked_by` deps are done).

**Recommended "done-right" order** (leaf stubs before the route cards that expose them):

| # | Card | P | Why this order |
|---|------|---|----------------|
| 1 | **0066** lock TTL sweep | P1 | correctness bug (lock leak); small, self-contained |
| 2 | **0048** ONNX embedder real inference | P2 | unblocks smart-search routes; `ort` dep already present |
| 3 | **0055** voice STT/TTS backends | P2 | **blocks 0062** voice routes |
| 4 | **0067** Junie CLI Task exec | P2 | small CLI wiring |
| 5 | **0069** TLS/mTLS or retire | P2 | security surface SPEC promises |
| 6–13 | **0056 EPIC → 0057,0059 (P1); 0058,0060,0061 (P2); 0062,0063 (P3)** | route coverage; all prereq stubs (0047/0049/0051/0052/0053/0054) now done — only 0062 waits on 0055 |
| 14 | **0064** add Default-Features to required CI checks | P1 | ⚠️ GitHub-branch-protection work — owner is migrating off GitHub; keep tracked, low urgency |
| 15–18 | **0065** Python/libloading hygiene; **0068** TUI; **0070** tokenizers; **0071** sqlcipher+ffi | P3 | complete-or-formally-retire (no-downgrade) |

## 6. Standing constraints for whoever picks this up
- **NO-DOWNGRADE** (owner, hard rule): a stub / hollow feature is an INCOMPLETE feature to
  COMPLETE — never silently remove or gate out. The "or formally retire with a recorded
  rationale" option on 0068/0069/0070/0071 is a *deliberate, documented* decision, not a skip.
- **Trust boundary:** any crypto stays pure-Rust (no C SQLCipher/OpenSSL/aws-lc); rustls ring-only.
- **Workflow (2026-06-17 owner directive):** moving off GitHub — bypass the PR-per-change dance;
  admin-merge / push direct to `main`, still guardian-verified. CI is slow (~25 min); don't
  long-poll it.
- **Toolchain quirk:** `~/Desktop/meta/.tool-versions` pins a missing rust → spurious E0514 on
  `--all-features`. Prefix cargo with
  `env -u __MISE_DIFF -u __MISE_SESSION -u __MISE_ORIG_PATH -u MISE_SHELL RUSTUP_TOOLCHAIN=1.96.0-x86_64-unknown-linux-gnu`.

## 7. Housekeeping noted (not done this session)
Stale merged worktrees still registered: `prompt_hub-phtask-0049`, `.worktrees/{commit-ledger,
fix-main-ci,phtask-0051,phtask-0052,phtask-0053,phtask-0054}`. Prune with
`git worktree remove` once confirmed merged (their branches are all in `main`).

## 8. Resume
```bash
cd ~/Desktop/meta/prompt_hub
hf resume                 # authoritative state
hf claim PHTASK-0066      # next recommended
```
